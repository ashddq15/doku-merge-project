# railway.toml
[build]
builder = "NIXPACKS" # atau "DOCKERFILE" kalau pakai Dockerfile

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"

[service]
healthcheckPath = "/debug/health"
healthcheckTimeout = 100
